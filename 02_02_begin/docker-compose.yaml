version: '3.7'
services: 
  website:
    build: 
      context: .
    ports:
     - 8080:80    
  unite-test:
    volume:
      - "$PWD:/app"
    build:
      context: .
      dockerfile: rspec.dockerfile


